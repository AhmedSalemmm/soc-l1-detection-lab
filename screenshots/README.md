# Screenshots

Captures to add as the lab VMs are rebuilt. Each screenshot should show the named state with no real hostnames, usernames, or IP addresses from any non-lab environment visible.

Planned captures:

- `wazuh-dashboard-overview.png`: Wazuh home dashboard with the lab agent reporting in.
- `wazuh-agent-active.png`: agent list showing win-endpoint-01 active.
- `wazuh-bruteforce-alerts.png`: alert list filtered to authentication failures during a lock-screen test.
- `wazuh-mitre-view.png`: the MITRE ATT&CK module showing mapped techniques from lab activity.
- `sysmon-event-viewer.png`: Event Viewer open on Sysmon/Operational showing an event 1 with full command line.
- `parser-output.png`: terminal output of `python parser/triage.py` against the sample logs.
